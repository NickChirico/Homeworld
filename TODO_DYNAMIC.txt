###############################################################################
# DONE #
###############################################################################

- Camera Control Scheme




<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<
<<< IN-PROGRESS <<<
<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

	- Create Pathfinding logic for a unit using GridBehavior's `path`
	- `Pathfinder` class should be separate from the `Unit` class






###############################################################################
# TODO #
###############################################################################

- Unit Control, like movement, up to last time
	- Click to move, Shift-Click to queue
	- NavAgent that keeps getting its dest set to the next `GridSpace` in its `Path`

- `GridSpace` and `GridPath` data structures
	- GOAL: Unit can R-click `MoveTo(GridSpace clickedSpace)`
	- ####: Find `BestPath` from currentSpace to clickedSpace
	- ####: The `GridManager` returns a `Path`(GridSpace[] struct)
	- ####: Unit follows that Path from GridSpace to GridSpace
	- ####: Unit can interact with the destination GridSpace once it reaches it...
		- Such as: Build a Structure, Root itself, Attack, Harvest, Etc.


- Create Grid-Based Play Area 
	- small arenas/islands with small tasks --> Exploratory Missions
	- large open area that needs to be explored --> Home Base

- Redownload AssetForge and make some debug tile sets + units 
